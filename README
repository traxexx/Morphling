
Mobile Element Insertions (MEI) detection. Calculate Genotype Likelihood from re-mapping reads to modified reference fasta (Will upload modified fasta soon).

Usage:

Morphling -SampleList <sample list> -OutVcf <output>

Sample list format:
	sample_name bam depth read_length avr_ins_size avr_ins_size_variation
