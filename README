Note: new version is coming soon with simplified usage and less time cost :)

Mobile Element Insertions (MEI) detection. Calculate Genotype Likelihood from re-mapping reads to modified reference fasta.


Usage: Discover --> Genotype --> Assembly (--> AltSequence)

Morphling Discover Usage:
  Morphling Discover -Bam <raw-bam> -WorkDir <working directory> -Mapper <full path of mapper> -GenomeFasta <reference fasta> -Sample <sample name>

Morphling Genotype Usage:
  Morphling Genotype -SampleList <list of samples> -WorkDir <genotyping working directory>


